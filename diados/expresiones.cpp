//expresion matematica como expresion en c++
/*#include<iostream>

using namespace std;

int main() {
	float a, b, resultado=0, c=2, d=4, rest=0;//lo igualas a 0 para no tener con basura

	cout << "Didite el valor de a: "; cin >> a;
	cout << "Didite el valor de b: "; cin >> b;

	resultado = (a / b) + 1;//() no importa por orden 
	rest = (a + b) / (c + d);
	//redondear a dos decimales 
	cout.precision(2);// solo salen dos numeros dspues de la coma digital
	cout << "\nMi resultado es "<< resultado<<endl;
	cout << "\nMi resultado es " << rest << endl;
	return 0;
}*/